DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/sdc_jobfair
PORT=3000
JWT_SECRET=CHANGE_ME_to_a_long_random_string
SERVER_SECRET=CHANGE_ME_different_from_JWT_SECRET
REDIS_URL=redis://localhost:6379
# Comma-separated allow-list for CORS (HTTP + Socket.IO). Defaults to the
# Vite dev server if unset — see lib/corsConfig.js.
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
